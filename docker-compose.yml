version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant:v1.9.1
    container_name: claritas-qdrant
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_data:/qdrant/storage
    restart: unless-stopped

